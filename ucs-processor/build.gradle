import org.gradle.internal.jvm.Jvm

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'maven-publish'

ext {
    ARTIFACT_ID="ucs-processor"
    NAME="UCS Annotation Processor"
    PACKAGING='jar'
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

dependencies {
    compile 'com.squareup:javapoet:1.9.0'
    compile 'org.checkerframework:checker:2.1.13'

    compile project(path: ':ucs-library')
}

apply from: "../maven-publish.gradle"